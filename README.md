# Online Voting System

This repository contains a full-stack Online Voting System.

## Structure

- `backend/`: Django backend with Django REST Framework, JWT authentication, and admin APIs.
- `frontend/`: React frontend with animations, theme toggle, and multilingual support.

---

## Backend Setup

1. Open terminal in `backend/`
2. Install dependencies:
   ```bash
   pip install -r requirements.txt